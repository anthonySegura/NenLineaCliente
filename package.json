{
  "name": "n-en-linea",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "actioncable": "^5.1.4",
    "react": "^15.6.1",
    "react-bootstrap": "^0.31.3",
    "react-dom": "^15.6.1",
    "react-google-login": "^2.9.3",
    "react-media": "^1.6.1",
    "react-notifications": "^1.4.3",
    "react-redux": "^5.0.6",
    "react-scripts": "1.1.5",
    "redux": "^3.7.2"
  },
  "scripts": {
    "start": "react-scripts start && cp manifest.json build/ && sw-precache --root = 'build/' --static-file-globs='build/**/!(*map*)'",
    "build": "react-scripts build && cp manifest.json build/ && sw-precache --root = 'build/' --static-file-globs='build/**/!(*map*)'",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "sw-precache": "^5.2.0"
  }
}
